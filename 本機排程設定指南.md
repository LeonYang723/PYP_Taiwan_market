# 本機排程設定指南——用自己的電腦/家用網路測試自動抓取

## 這份指南要解決什麼問題

原本的自動化爬蟲跑在 GitHub Actions 的雲端機房裡，三個版本都被蝦皮擋下，判斷是**整個雲端機房的出口 IP／裝置指紋已被蝦皮整批封鎖**。這是一個值得實測的假設：如果換成從**你自己的電腦、家用網路 IP** 執行同一支爬蟲，有機會繞過這個封鎖（因為家用網路 IP 的信譽通常比雲端機房高很多）。

這份指南教你怎麼在自己的 Windows 電腦上跑這支爬蟲。**請先照 Step 1～3 手動測試一次，確認真的抓得到資料，再考慮 Step 4 設定自動排程**——如果家用網路一樣被擋，代表蝦皮的偵測不只看 IP，這條路就走不通，維持人工蒐集流程即可，不需要浪費力氣設定排程。

## Step 1：安裝需要的軟體

### 1-1 安裝 Python

1. 前往 <https://www.python.org/downloads/> 下載最新版 Python（3.11 以上皆可）
2. 安裝時**務必勾選「Add python.exe to PATH」**這個選項——沒勾的話後面的指令都會找不到 `python`
3. 安裝完成後，按 `Win` 鍵，打「命令提示字元」開啟，輸入：

   ```
   python --version
   ```

   看到版本號（例如 `Python 3.12.4`）就代表裝好了。

### 1-2 安裝 GitHub Desktop

1. 前往 <https://desktop.github.com/> 下載安裝
2. 打開後用你的 GitHub 帳號登入
3. 左上角 **File → Clone Repository**，選你的 `PYP_Taiwan_market` repo，選一個要存在電腦上的資料夾（例如 `C:\Users\你的名字\Documents\PYP_Taiwan_market`），按 **Clone**
4. Clone 完成後，那個資料夾裡就會有完整的 repo 內容（`scraper/`、`scripts/`、`local_run/` 等等都在裡面）

> 用 GitHub Desktop 而不是純指令列 git，是因為它有圖形介面可以直接看到「哪些檔案變動了」、按按鈕就能 commit/push，跟你原本習慣的網頁編輯操作邏輯比較接近，不需要背指令。

## Step 2：一次性環境設定

打開命令提示字元，切換到剛剛 clone 的資料夾（路徑換成你實際存放的位置）：

```
cd C:\Users\你的名字\Documents\PYP_Taiwan_market
```

安裝這支爬蟲需要的套件（只需要做一次）：

```
pip install -r requirements.txt
playwright install --with-deps chromium
```

`playwright install` 這一步會下載瀏覽器執行檔，可能要跑幾分鐘，等游標回到命令提示字元就是完成了。

## Step 3：先手動測試一次（重要，不要跳過）

**在正式排程之前，一定要先手動跑一次，確認家用網路真的沒有被擋。**

在檔案總管裡打開 repo 資料夾，進到 `local_run` 子資料夾，雙擊 **`run_scraper.bat`**（或在命令提示字元裡輸入 `local_run\run_scraper.bat`）。

第一次執行可能要 3～5 分鐘，跑完後會自動打開一個提示視窗，接著打開 `local_run\logs\latest_run.log` 這個檔案看結果：

- **看到類似「關鍵字 'PYP' 抓到 87 筆商品」這種訊息，結尾沒有「爬蟲結束碼」的警告** → 家用網路的 IP 沒有被擋，可以往下做 Step 4。
- **看到「所有關鍵字都沒有抓到任何資料，很可能已被蝦皮封鎖/擋下」，或是 log 裡出現 `error 欄位=90309999`** → 家用網路一樣被擋，這條路目前走不通。這種情況請把 `latest_run.log` 的內容回報給我，我幫你判斷下一步（例如要不要考慮住宅 IP 代理服務），並且維持原本的人工蒐集流程就好，不用往下做 Step 4。

## Step 4：設定 Windows 工作排程器，自動定期執行

**只有 Step 3 測試成功、真的抓到資料，才需要做這一步。**

1. 按 `Win` 鍵，搜尋「工作排程器」(Task Scheduler)，打開
2. 右側點「建立基本工作」
3. 名稱填「PYP蝦皮爬蟲」，下一步
4. 選擇觸發頻率——**建議「每週」或「每月」，不要每天**，一方面配合季報不需要抓那麼密集，另一方面降低被偵測到「規律性機器人行為」的機率
5. 動作選「啟動程式」，下一步
6. 「程式或指令碼」填入 `run_scraper.bat` 的完整路徑，例如：

   ```
   C:\Users\你的名字\Documents\PYP_Taiwan_market\local_run\run_scraper.bat
   ```

7. 完成

之後它就會照排程自動在背景執行，**前提是那個時間點電腦要是開機且有連網**——電腦關機或休眠時排程不會執行，這是本機排程跟雲端排程最大的差異，之後如果發現連續好幾次都沒有新的 log，先檢查是不是排程時間電腦剛好沒開。

## Step 5：定期把結果同步到 GitHub

這一步刻意保留成手動的，讓你每次都能看一眼有什麼變化再決定要不要上傳：

1. 打開 GitHub Desktop
2. 左側「Changes」清單如果出現 `data/raw/` 或 `docs/data/` 底下的新檔案/變動 → 代表本機排程有跑出新資料
3. 下面「Summary」欄位隨便寫一句描述（例如「本機排程更新資料」）
4. 按左下角 **Commit to main**
5. 按右上角 **Push origin**

儀表板網站過一兩分鐘就會更新。建議至少每次排程跑完後，找時間打開 GitHub Desktop 看一下有沒有新資料要推上去。

## 之後想讓同步也自動化（進階，選用）

如果手動按 GitHub Desktop 覺得麻煩，也可以讓 `run_scraper.bat` 自動幫你 `git add` / `commit` / `push`，但這需要先在電腦上設定好 git 的登入憑證（例如安裝 Git for Windows，並設定 GitHub 的 Personal Access Token 供指令列使用）才能在沒人手動輸入帳密的情況下自動推送。這部分設定好之後可以告訴我，我再幫你把批次檔加上自動同步的部分。

## 小提醒

- `local_run\logs\latest_run.log` 每次執行都會被覆蓋，只留最新一次的紀錄；`local_run\logs\history.log` 是所有執行紀錄的累積檔案，方便回頭查之前的結果。
- 如果測試或排程執行後不確定結果是好是壞，把 `latest_run.log` 的內容整個貼給我，我可以幫你判斷。
- 這套本機排程方式跟人工填 Excel 蒐集資料**可以並存**——就算本機排程測試成功、能穩定抓到商品層級的總銷量，「規格別市場需求推估」目前還是要靠人工看評價統計，這部分不受這次測試影響。
